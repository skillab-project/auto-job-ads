# ===========================
# SKILLAB AUTO JOB ADS CONFIG
# ===========================

# --- LLM server connection ---
API_URL=<REPLACE_ME>                       # Ollama/OpenWebUI or other LLM endpoint
API_TOKEN=<REPLACE_ME>                     # Optional if local
MODEL_NAME=mistral:latest

# --- LLM behaviour ---
TEMPERATURE=0.1
SEED=42
TIMEOUT=1200

# --- Logging & metadata ---
SERVICE_NAME=auto-job-ads
LOG_LEVEL=info
PORT=8000
